apiVersion: v1
kind: Service
metadata:
  namespace: log-namespace
  name: dwk-pingpongapp-svc
spec:
  type: LoadBalancer
  selector:
    app: dwk-pingpongapp
  ports:
    - port: 80
      protocol: TCP
      targetPort: 9002